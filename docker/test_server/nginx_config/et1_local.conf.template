server {

       listen *:${SERVER_PORT};
       server_name et1.${SERVER_DOMAIN} et1.et;
        location / {
           proxy_pass ${MY_ET1};
           proxy_set_header Host $http_host;
        }

}
